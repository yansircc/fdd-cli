# Flows

<!--
规则：
1. 禁止 Mermaid/ASCII 图表，用纯文本描述
2. 每个流程用单行箭头表示核心路径
3. 只记录"做什么"，不记录"怎么实现"
-->

## F1: {{flow-name}}

```
输入 → 处理 → 输出
```

**触发**：{{何时触发}}
**结果**：{{预期结果}}

## F2: {{flow-name}}

```
输入 → 处理 → 输出
```

**触发**：{{何时触发}}
**结果**：{{预期结果}}
